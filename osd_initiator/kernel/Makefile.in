
#ifneq ($(KERNRELEASE),)

obj-m := suo.o

#else

KDIR := @KDIR@

EXTRA_CFLAGS += -g

all:
	$(MAKE) SUBDIRS=$(PWD) -C $(KDIR) modules 

clean:
	$(MAKE) SUBDIRS=$(PWD) -C $(KDIR) clean

#else

tags: FORCE
	ctags suo.c suo_ioctl.h
FORCE:;

#endif
